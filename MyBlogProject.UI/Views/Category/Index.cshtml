@model IEnumerable<MyBlogProject.Entity.Category>

@{
    ViewBag.Title = "Index";
}

<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
<link href="https://fonts.googleapis.com/css?family=Roboto:300,400&display=swap" rel="stylesheet">

<link rel="stylesheet" href="~/Content/css/css_table/fonts/icomoon/style.css">

<link rel="stylesheet" href="~/Content/css/css_table/owl.carousel.min.css">

<!-- Bootstrap CSS -->
<link rel="stylesheet" href="~/Content/css/css_table/bootstrap.min.css">

<!-- Style -->
<link rel="stylesheet" href="~/Content/css/css_table/style.css">





<br />
<br />

<div class="table-responsive custom-table-responsive">

    <table class="table custom-table">
        <thead>
            <tr>
                <div class="form-group">
                    <div class="col-mx-10">
                        <button type="submit" class="btn btn-outline-dark border-0">
                            <span class="bi bi-plus-circle"></span> Create
                        </button>
                    </div>
                </div>
            </tr>
        </thead>

        <thead>
            <tr>

                <th scope="col"> @Html.DisplayNameFor(model => model.CreateOn)</th>
                <th scope="col"> @Html.DisplayNameFor(model => model.ModifiedOn)</th>
                <th scope="col"> @Html.DisplayNameFor(model => model.ModifiedUsername)</th>
                <th scope="col"> @Html.DisplayNameFor(model => model.Title)</th>
                <th scope="col"> @Html.DisplayNameFor(model => model.Description)</th>
                <th scope="col">   </th>
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.CreateOn)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ModifiedOn)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ModifiedUsername)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Title)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>

                        <a href="/Category/Edit/@item.Id" class="btn btn-warning btn-sm" title="Edit">
                            <span class="bi bi-pencil-square"></span>
                        </a>
                        <a href="/Category/Details/@item.Id" class="btn btn-info btn-sm" title="Details">
                            <span class="bi bi-clipboard"></span>
                        </a>
                        <a href="/Category/Delete/@item.Id" class="btn btn-danger btn-sm" title="Delete">
                            <span class="bi bi-eraser"></span>
                        </a>

                    </td>
                </tr>
                <tr class="spacer"><td colspan="100"></td></tr>
            }

        </tbody>
    </table>
</div>









<script>
    $(document).ready(function () {
        $('#footer').hide();
    });
</script>
